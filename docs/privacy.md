# Privacy

The extension processes prompt text locally to identify likely secrets. Raw prompts, source code, credentials, and detected values must never be sent to or stored by the backend.

The future event pipeline may store metadata such as category, severity, action, service, extension version, and timestamp. Telemetry must remain configurable and retention must be minimized.
