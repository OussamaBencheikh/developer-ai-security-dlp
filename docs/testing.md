# Testing

Detection and redaction tests use synthetic values only. Required CI gates are typecheck, unit tests, extension build, API tests, authorization tests, dependency audit, and security regression tests.

The current environment could not run these commands because Node.js/npm is not available on PATH; install Node.js 20+ before relying on the test status.
