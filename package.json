{
  "name": "developer-ai-security-dlp",
  "private": true,
  "version": "0.1.0",
  "description": "Privacy-first developer DLP for AI browser workflows.",
  "workspaces": ["packages/*", "apps/*", "services/*"],
  "scripts": {
    "build": "npm run build -ws --if-present",
    "typecheck": "npm run typecheck -ws --if-present",
    "test": "npm run test -ws --if-present",
    "lint": "npm run lint -ws --if-present"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "@types/chrome": "^0.0.287",
    "esbuild": "^0.24.2",
    "typescript": "^5.7.2",
    "vitest": "^2.1.8"
  }
}
